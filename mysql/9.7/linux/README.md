# AxialDB for MySQL 9.7 - Linux x64 eval (planned)

**Not yet released.**

Target platform for first Linux eval: **Ubuntu 24.04 LTS x64** with MySQL Community **9.7**.

When available, the bundle will match the Windows layout: plugin `.so` files, `axialdb-engine` binary, example `axialdb.toml`, UDF SQL, `VERSION`, and this README with FHS install paths.

See [Releases](https://github.com/AxialDB/releases/releases) for published builds.
